# Dev/test dependencies (not needed to run the toolchain).
#   pip install -r requirements-dev.txt
#   pytest -q
-r requirements.txt
pytest>=7
# handbook PDF build (tools/build_handbook.py)
weasyprint>=60
markdown

