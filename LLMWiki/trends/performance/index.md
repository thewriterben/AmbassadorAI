# Performance reports

Newest first. Generated by `tools/dgd_performance.py report` from the post ledger
(`ledger.jsonl`). These turn published-post metrics into evidence for hook/topic
choices — a craft signal, kept educational and never promotional.

_No reports yet — the ledger is empty and no weekly task is configured (checked 2026-07-19)._

> Until this has data, the agent's "what's actually working" guidance (SKILL.md Job C) is **inference, not evidence**. Record posts with `dgd.py perf record …`, then `dgd.py perf report`.
